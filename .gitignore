# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
venv/
env/
.vscode/
.idea/
.ipynb_checkpoints/
.DS_Store
Thumbs.db
data/*.csv

# Trained artifacts are rebuilt by running the benchmark rather than committed.
# A pickle executes on load, so shipping one asks a reader to trust a binary in
# order to inspect the repository.
results/*.pkl
results/*.joblib
